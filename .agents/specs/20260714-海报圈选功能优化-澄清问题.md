# 海报圈选功能优化 - 需求澄清问题清单

> 生成时间: 2026-07-14
> 状态: 待澄清

---

## 一、数据结构澄清

### Q1. 全局圈选 vs 图灵圈选的关系
**背景**: 需求提到"全局圈选 > 原图灵圈选"，但未明确是继承/替代还是独立功能。
- 全局圈选是否复用现有图灵圈选的数据表？
- 原图灵圈选功能是否保留？两者是否可同时配置？

### Q2. 黑白名单数据模型设计
**背景**: 新增设备黑名单、设备白名单，需确认存储方式。
- 黑白名单是独立表还是字段扩展？
- 设备黑白名单与现有门店黑白名单是否共用同一数据结构？
- 字段设计建议：`type` (全局/设备黑/设备白)、`bizTid`、`storeId`、`deviceId`

### Q3. 圈选维度字段定义
**背景**: 需求提到"门店和设备维度多选"，但当前系统可能只有单维度。
- 当前圈选表是否已有维度字段？
- 是否需要新增维度枚举：`GLOBAL`(全局)、`STORE`(门店)、`DEVICE`(设备)？

---

## 二、接口设计澄清

### Q4. 图灵ID批量查询接口
**背景**: 需求支持20个ID批量查询，需确认接口设计。
- 是否复用现有图灵平台接口？还是新增批量查询API？
- 单次查询上限：需求说20个，是否需要校验并返回错误提示？
- 批量查询失败时：部分成功如何处理？是否返回成功列表+失败ID？

### Q5. 活动ID校验接口
**背景**: 需求要求活动ID可校验出活动名称（类似奖品ID）。
- 是否已有活动查询接口可复用？
- 校验时机：输入时实时校验还是点击"查询"按钮？
- 返回字段：活动名称、活动状态、有效期？

### Q6. 黑白名单二次修改接口
**背景**: 需求提到"发布后二次修改"，需确认权限和操作逻辑。
- 修改接口是否复用创建接口（PUT覆盖）？
- 是否需要新增操作日志表？
- 是否需要审批流程？

---

## 三、前端交互澄清

### Q7. 多ID输入与分页展示
**背景**: 需求要求"英文逗号分隔，自动识别，最多20个，前端分页展示"。
- 分页组件：每页显示几条？是否支持搜索筛选？
- 已输入ID超过20个时：是截取前20个还是清空并提示错误？
- 粘贴时自动识别逗号分隔：是否需要去重？是否需要去除空格？

### Q8. 查询结果展示
**背景**: 多个ID查询结果需展示"biztid、标签名称、设备数量、有效期"。
- 多个结果时：是否分页？每页几条？
- "查看更多"交互：是弹窗分页还是展开显示？
- 查询失败时：部分ID无数据如何展示？

### Q9. 黑白名单配置回显
**背景**: 编辑时需回显创建时配置的信息。
- 回显时机：页面加载时自动查询还是点击"查询"按钮？
- 发布后二次修改：是否需要展示修改历史？

---

## 四、业务逻辑澄清

### Q10. 黑白名单优先级规则
**背景**: 设备黑名单、设备白名单、全局圈选同时配置时的优先级。
- 优先级规则：白名单优先还是黑名单优先？
- 冲突处理：同一设备同时出现在黑白名单如何处理？

### Q11. 圈选有效期同步
**背景**: 图灵平台的人群有有效期，需确认本地处理逻辑。
- 有效期过期后：是否自动失效？是否需要提前提醒？
- 本地是否缓存圈选数据？缓存时长？

### Q12. 立减活动信息回显
**背景**: "中心化立减进度小程序和整点立减活动海报"配置立减ID。
- 是否是两个不同模块？各需什么字段？
- 活动信息展示：活动名称、活动时间、活动状态？

---

## 五、技术实现建议（待确认）

### 建议1: 数据模型扩展
```sql
-- 圈选表扩展
ALTER TABLE poster_selection ADD COLUMN dimension VARCHAR(20) COMMENT '维度: GLOBAL/STORE/DEVICE';
ALTER TABLE poster_selection ADD COLUMN selection_type VARCHAR(20) COMMENT '类型: GLOBAL/DEVICE_BLACKLIST/DEVICE_WHITELIST';
```

### 建议2: 批量查询接口设计
```json
POST /api/turing/crowd/batch-query
Request: { "bizTids": ["id1", "id2", ...], "maxCount": 20 }
Response: {
  "success": true,
  "data": [
    { "bizTid": "id1", "name": "标签名", "deviceCount": 100, "expireTime": "2026-08-01" }
  ],
  "failedIds": ["id3"]
}
```

### 建议3: 前端分页组件
- 每页10条
- 支持搜索过滤
- 失效数据用灰色标记

---

## 六、待确认优先级

| 问题编号 | 优先级 | 理由 |
|---------|--------|------|
| Q1-Q3 | P0 | 影响数据库设计，需先确认 |
| Q4-Q6 | P0 | 影响接口定义，前端依赖 |
| Q7-Q9 | P1 | 前端交互细节，可后续调整 |
| Q10-Q12 | P1 | 业务规则，影响逻辑实现 |

---

## 七、后续行动

1. **产品确认**: 请产品经理对Q1-Q12进行确认，提供原型图补充说明
2. **技术评审**: 待确认后，技术团队进行接口设计和数据库变更评审
3. **排期评估**: 评估开发工时，制定迭代计划

---

*本文档由自动化澄清流程生成，请相关方在2个工作日内完成确认*